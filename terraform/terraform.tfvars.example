# InsureMail AI Terraform Variables
# Copy this file to terraform.tfvars and customize for your environment

# Project Configuration
project_name = "insuremail-ai"
environment  = "dev"
aws_region   = "us-east-1"

# Lambda Configuration
lambda_runtime = "python3.11"
log_retention_days = 7

# Amazon Bedrock Models (Cost-Optimized Configuration)
# Using only the most cost-effective models
bedrock_models = [
  # Claude 3 Haiku - Most cost-effective Claude model (10x cheaper than Sonnet)
  # $0.25 per 1M input tokens, $1.25 per 1M output tokens
  # Perfect for production use with insurance emails
  "anthropic.claude-3-haiku-20240307-v1:0",

  # Amazon Titan Embeddings - For RAG (cheapest embedding model)
  # $0.10 per 1M tokens
  "amazon.titan-embed-text-v1"

  # Note: Removed expensive models for cost optimization:
  # - Claude 3 Sonnet: $3/$15 per 1M tokens (10x more expensive)
  # - Claude 3 Opus: $15/$75 per 1M tokens (50x more expensive)
  # See docs/COST_OPTIMIZATION.md for detailed comparison
]

# SES Configuration (Real Email Integration)
# IMPORTANT: You must verify this email address in SES before deploying
# Run: ./scripts/setup_ses.sh to automate verification
sender_email = "support@yourdomain.com"
sender_name  = "InsureMail AI Support"

# Optional: Filter which emails to receive
# Empty list = receive all emails to this domain
# Specific list = only receive emails to these addresses
ses_receipt_recipients = []
# Example with filtering:
# ses_receipt_recipients = ["support@yourdomain.com", "claims@yourdomain.com"]

# Monitoring Configuration
enable_bedrock_logging = true

# Resource Tags
tags = {
  Project    = "InsureMail AI"
  ManagedBy  = "Terraform"
  Repository = "auto_email_classification_prod"
  Environment = "dev"
}
